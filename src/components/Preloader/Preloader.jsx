import React from "react";
import loading from "../../images/loading.gif";

let Preloader = (props) => {
    return(
    <div>
        <img src={loading}/>
    </div>
    )
}

export default Preloader